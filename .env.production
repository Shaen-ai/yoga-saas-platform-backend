# Production Environment Variables
NODE_ENV=production
PORT=8000

# Database - REQUIRED for production
# Use MongoDB Atlas (https://cloud.mongodb.com) or your own MongoDB instance
MONGODB_URI=mongodb+srv://yoga_app_db_user:yn7F2Q5wvMkLCJHw@cluster.pyjnbkm.mongodb.net/yoga_saas?retryWrites=true&w=majority
SKIP_DB=false  # Must be false in production

# Authentication - REQUIRED
JWT_SECRET=generate-a-strong-random-secret-key-here-use-crypto-randomBytes-64
BYPASS_AUTH=false  # Must be false in production

# Wix Authentication - REQUIRED
# Already configured with your credentials
WIX_APP_ID=74a1061c-62ad-4926-94d7-ef7a94bc1330
WIX_APP_SECRET=b3fb45b9-277e-4722-a724-69d51ac6a82f
SKIP_WIX_AUTH=false  # Must be false in production

# Email Service (optional but recommended)
EMAIL_SERVICE=gmail
EMAIL_USER=your-production-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# API Configuration
API_BASE_URL=https://your-api-domain.com

# CORS Settings (optional - add your production domains)
ALLOWED_ORIGINS=https://your-domain.com,https://your-app.wixsite.com

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # per window

# Logging
LOG_LEVEL=info  # error, warn, info, debug